## Getting Started

Welcome to the VS Code Java world. Here is a guideline to help you get started to write Java code in Visual Studio Code.

## Folder Structure

The workspace contains two folders by default, where:

- `src`: the folder to maintain sources
- `lib`: the folder to maintain dependencies

Meanwhile, the compiled output files will be generated in the `bin` folder by default.

> If you want to customize the folder structure, open `.vscode/settings.json` and update the related settings there.

## Dependency Management

The `JAVA PROJECTS` view allows you to manage your dependencies. More details can be found [here](https://github.com/microsoft/vscode-java-dependency#manage-dependencies).

## 🚀 Guía de Uso Rápido de GetClasses

Esta sección detalla los pasos para compilar, ejecutar e iniciar sesión en la aplicación.

---

### 🛠️ Requisitos

Asegúrate de tener instalado el **Java Development Kit (JDK)** en tu sistema para poder compilar y ejecutar el código fuente.

---

### 💻 Pasos de Ejecución

Sigue las siguientes instrucciones para iniciar la aplicación desde la línea de comandos (terminal):

#### 1. Compilación del Código

1.  Navega hasta el directorio raíz donde se encuentran los archivos `.java` (por ejemplo, dentro de la carpeta `/GetClass`).
2.  Compila los archivos fuente (esto puede variar si usas un IDE o un *build tool* como Maven o Gradle):

    ```bash
    javac *.java
    ```

#### 2. Iniciar la Aplicación

1.  Ejecuta el archivo principal, **`App.java`**:

    ```bash
    java App
    ```

    > *Nota: También puedes ejecutar la aplicación directamente desde tu IDE preferido (Eclipse, IntelliJ, NetBeans, etc.).*

---

### 🔑 Inicio de Sesión de Prueba (Login)

Una vez que la interfaz de la aplicación se haya cargado:

1.  Utiliza las siguientes credenciales de prueba para iniciar sesión:

| Campo | Valor |
| :--- | :--- |
| **Email** | `AEscobar@gmail.com` |
| **Contraseña** | `AlexPrueba1609` |

2.  Haz clic en el botón **"Iniciar Sesión"**.
3.  Serás redirigido a la **vista principal (Main)** de la plataforma.
